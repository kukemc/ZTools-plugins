# Kuke AI Agent

A ZTools plugin that provides a local AI Agent with tool-use capabilities: file operations, shell commands, web search, and more. Supports any OpenAI-compatible model.

## Development

```bash
npm install
npm run dev      # Start dev server on port 5173
npm run build    # Typecheck + build to dist/
npm run pack     # Build production zip for ZTools
```
